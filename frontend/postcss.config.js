module.exports = {
  plugins: {
    // 1. Tailwind CSS: This plugin processes your CSS and compiles Tailwind utility classes.
    'tailwindcss': {},
    
    // 2. Autoprefixer: This plugin automatically adds vendor prefixes 
    //    (like -webkit-, -moz-) to your CSS rules, ensuring compatibility 
    //    across different web browsers.
    'autoprefixer': {},
  },
};